import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gh-24482-date-swipe',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
